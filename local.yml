---
- hosts: localhost # running the playbook against the host machine
  connection: local #use local connection -> no ssh
  become: true # using sudo

  tasks:
  - name: install htop and nvim
    package:
      name:
        - htop
        - neovim

  - name: copy wallpaper file
    copy:
      src: files/Horizon_single.jpg
      dest: /usr/share/backgrounds/Horizon_single.jpg
      owner: root
      group: root

  - name: set wallpaper
    dconf:
      key: "/org/gnome/desktop/background/picture-uri"
      value: "'file:///usr/share/backgrounds/Horizon_single.jpg'"


  - name: set wallpaper position
    dconf:
      key: "/org/gnome/desktop/background/picture-options"
      value: "'zoom'"
